# #加载驱动
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# #数据库连接路径
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/backendData?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=68686868

# 配置端口
server:
  port: 8085

spring:
  # 配置数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/backendData?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 68686868
